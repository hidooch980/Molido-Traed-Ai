# وضعیت پروژه — MolidoTrade AI

*آخرین به‌روزرسانی: ۲۲ شهریور ۱۴۰۵ (13 Sep 2026)*

## خلاصهٔ یک‌خطی

هر ۵۳ فاز ساخته و تست شده است؛ **لبهٔ سودده هنوز اثبات نشده**. در شب ۶ به ۷
شهریور کل ناوگان از حساب‌های دموی MetaQuotes به شش حساب دموی RoboForex-ECN
منتقل شد، ژورنال به درخواست مالک از صفر شروع شد، و یک حساب دموی چالش FTMO
اضافه شد. تا ۱۳ سپتامبر چهار حساب از هفت روی trend-following‌اند. حساب واقعی همچنان هیچ.

## اعداد (گزارش سلامت روی سرور، ۱۳ سپتامبر ۰۵:۱۴ UTC)

| سنجه | مقدار |
|---|---|
| فازهای مشخصات | ۵۳ / ۵۳ |
| تست | ۳٬۶۳۰ سبز (شمارش ۷ سپتامبر؛ دوباره شمرده نشد) |
| کندل ذخیره‌شده | ۱٬۸۸۰٬۷۶۰ |
| مقدار ویژگی | ۶٬۴۲۶٬۸۳۲ |
| ردیف ژورنال | ۱۸٬۴۷۲ (از صفرِ ۶ شهریور) |
| episode | ۲۴۲٬۸۱۱ |
| نمونهٔ equity | ۴٬۷۵۰ |
| یافتهٔ تضاد منبع داده | ۹۸٬۵۳۸ |
| معاملهٔ حل‌شده | در گزارش سلامت نیست — شمرده نشد |

هر نُه چرخه `ok` بودند. آخرین تصمیم و آخرین حل‌شدن ۳۱ و ۲۷ ساعت پیش بود،
یعنی جمعه‌شب — بازار آخر هفته بسته است، پس این کهنگی انتظار است نه نقص.

**بار سرور — علت پیدا و نیمی رفع شد (۱۳ سپتامبر):** هر نماد Market Watch
در متاتریدر زیر Wine یک رشتهٔ `history` دارد که حتی با بازار بسته حدود ۱٪
یک هسته می‌خورد. پل ۴۱ نماد انتخاب می‌کرد، پس پنج ترمینال هر کدام ~۳۰٪ CPU
فقط برای انتظار مصرف می‌کردند. پل حالا ۲۴ نماد دارد (۲۰ ابزار معاملاتی +
NZDJPY، EURNZD، GBPCAD، AUDCAD) و بقیه را پنهان می‌کند؛ روی هر هفت ترمینال
نصب و کامپایل شد (۰ خطا، پشتیبان `.before-symbols` کنار هر کدام).

| سنجه | قبل (۰۵:۱۴–۰۸:۴۸ UTC) | بعد (۱۴:۱۰ UTC) |
|---|---|---|
| load average (۱ / ۵ / ۱۵ دقیقه) | ۵۸ / ۶۱ / ۶۲ | ۳۱ / ۳۶ / ۴۳ |
| فشار CPU (PSI some avg60) | ۵۲٪ | ۳۷٪ |
| CPU آزاد (vmstat id) | ۴۷–۴۹٪ | ۷۵–۷۹٪ |
| mt5d / mt5f / mt5g | ۳۴ / ۳۰ / ۲۶٪ | ۲۳ / ۲۲ / ۲۳٪ |

**هنوز باز:** چرخهٔ `collect` بهتر نشده — ۲۷۶ تا ۴۱۰ ثانیه در ۹۰ دقیقهٔ
آخر، که بیشترش پیش از پایان نصب بود؛ باید در چرخه‌های بعدی دوباره خوانده
شود. mt5j درست پس از ری‌استارت ۵۰٪ CPU داشت و خودش به build 6182 به‌روز شد.
mt5c و mt5e فقط ۴ فایل تاریخچه باز دارند با وجود ۲۴ نماد منتشرشده؛ علتش
بررسی نشده.

**ریسک هر معامله ۸٪ است** (تصمیم مالک، ۱۳ سپتامبر: ۰.۷۵ → ۱ → ۲ → ۴ → ۸)، روی هر
هفت حساب. در کانتینر collector خوانده و در گزارش سلامت تأیید شد. دو نیمه‌کردن
ایمنی (`calibrated`، `training_eligible`) عمداً مانده‌اند، پس حجم واقعی هر
سفارش ~۱٪ حساب است (~۲٬۰۰۰ دلار روی ۲۰۰ هزار). روی FTMO یعنی سه باخت در یک
روز به حد افت روزانهٔ ۳٪ می‌رسد. سقف پل از `MaxLots=5` به **۱۰** رفت (۱۴ سپتامبر،
روی هر هفت ترمینال کامپایل با ۰ خطا و ری‌استارت تمیز، پوزیشن‌ها پیش و پس یکسان).

**امنیت (۱۴ سپتامبر):** Caddy پشت ArvanCloud حالا فقط بازه‌های CDN را
`trusted_proxies` می‌داند و API با `MOLIDO_TRUSTED_PROXY_HOPS` آدرس واقعی را
می‌خواند؛ پیش از این همهٔ تلاش‌های ورود زیر یک آدرس داخلی شمرده می‌شدند
(قفل ورود برای همه). حدود ۶۸ ثانیه قطعی هنگام بازسازی caddy.

**نیروهای مجازی — روز ۱ آزمایشی (۱۴ سپتامبر، محیط تست محلی، اجرا خاموش):**
ورود + TOTP اجباری، ۱۰ صفحه، خروج، ماندگاری پس از ری‌استارت API: PASS.
مسدودکننده: هیچ. اصلاح‌شده: hydration ساعت‌ها (f7eb10b). باز: کوکی نشست
`secure=False` روی دامنهٔ HTTPS (`session.py:342`)، متن قدیمی داشبورد.
تغییر ریسک/مغز و آزمون‌های مهاجم A2، A5–A8 به روز ۲ ماند (نیاز به پل ساختگی).

**روز ۲ (۱۴ سپتامبر، پل ساختگی دمو):** ورود دوباره، تغییر ریسک/مغز و
ماندگاری پس از ری‌استارت، سقف ۵٪ در سرور (422)، بیننده روی هر مسیر نوشتنی
403، تزریق در نام escape شد، CSRF از سایت دیگر رد شد، لاگ بدون رمز: PASS.
اصلاح‌شده: کوکی نشست حالا `Secure` روی دامنه (منتشر شد، ۰۹:۰۸ UTC؛
`MOLIDO_SESSION_COOKIE_SECURE=true`)، `changed_by` تغییر ریسک همیشه خالی بود
(261918c، هنوز منتشر نشده)، نتیجهٔ جابجایی استاپ لاگ نمی‌شد (کامیت محلی،
منتشر نشده). همه تا ۱۵:۴۲ UTC منتشر شدند، به‌علاوهٔ هم‌زمانی پاسخ ورود برای
ایمیل ناموجود (ab77a3a)، متن و ساعت داشبورد، و دروازهٔ «نوع حساب» در صفحهٔ
خلبان خودکار که حالا هر ۷ ترمینال را می‌خواند (3d9f66a؛ داخل کانتینر: هر ۷
«demo»، باز). لاگ ۲۴ ساعتهٔ api برای رمز/توکن/hash: صفر.

**جابجایی استاپ روی سرور کار می‌کند:** ۷ از ۴۱ پوزیشن زنده استاپشان از قیمت
ورود گذشته (سود قفل‌شده)؛ بقیه هنوز استاپ 1R و حد سود 1.5R اولیه را دارند.
پشتیبان‌ها روی سرور: `.env.prod.bak-risk-20260913` (۰.۷۵)،
`.env.prod.bak-risk1-20260913` (۱)، `.env.prod.bak-risk2-20260913` (۲).

**مغز mt5d عوض شد:** `67209480` از stochastic-reversion (۱ برد از ۲۱ پوزیشن
مستقل) به **donchian-breakout** رفت (تصمیم مالک). پشتیبان:
`.env.prod.bak-mt5d-brain-20260913`.

**شمارش پوزیشن، نه ردیف:** ژورنال همان دید را هر چرخه دوباره ثبت می‌کند؛ ۹٬۲۳۳
ردیف فقط ۱۲۸ پوزیشن بسته‌شدهٔ مستقل بودند و ۶۵ «برد» momentum یک رالی نفت بود.
`weekly.build_report` حالا کنار شمارش ردیف‌ها `positions`، `positions_resolved`،
`position_wins` و `position_mean_r` می‌دهد (کامیت `547b6de`، روی collector و
chat نصب‌شده). هیچ مغزی به ۵۰ پوزیشن نرسیده؛ بیشترشان زیر ۲۰.

**تریلینگ استاپ** روی همهٔ حساب‌ها روشن است (`MOLIDO_TRAILING_LOGINS=*`): از
+۱R، ۰.۶ فاصلهٔ اولیه پشت بهترین قیمت؛ هرگز عقب نمی‌رود؛ تارگت جابه‌جا
نمی‌شود. مالک شروع از +۱R را نگه داشت.

**علت کندی چرخهٔ `collect` (۴۲۱ ثانیه):** `forward.snapshot` هر بار کل تاریخچهٔ
H1 (۵۲۹ هزار ردیف) را می‌خواند تا ۸۰ کندل آخر را نگه دارد — ۲۴ ثانیه اجرا +
۶ ثانیه برنامه‌ریزی، چند بار در هر چرخه. کامیت `182a9c5` پنجره را به سه برابر
lookback محدود می‌کند؛ روی دادهٔ واقعی ۰.۳ تا ۰.۸ ثانیه.

**نصب‌شده و اندازه‌گیری‌شده (۱۳ سپتامبر، چرخه‌های زمان‌بندی‌شده، نه چرخهٔ راه‌اندازی):**

| نسخه | کامیت | مدت چرخه |
|---|---|---|
| پیش از اصلاح | — | ۴۲۱ ثانیه |
| پنجرهٔ snapshot | `182a9c5` | ۲۰۸ ثانیه |
| بازهٔ زمانی برای بقیهٔ کوئری‌ها | `fe32fbe` | ۱۹۸ ثانیه |
| نسخهٔ دقیق | `ad45374` | ۲۱۱ ثانیه، ۰ خطا |
| فقط کندل‌های اخیر بروکر نوشته می‌شوند (فعلی) | `3e73de0` | **۵۸ ثانیه**، ۰ خطا |

**تصحیح `fe32fbe`:** پیام آن کامیت می‌گفت هیچ بازه‌ای نتیجه را عوض نمی‌کند؛
روی سرور نادرست بود. پنجرهٔ ۱۴ روزه نمادهایی را که ۸۰ کندل آخرشان عقب‌تر
می‌رود حذف می‌کرد (سهام آمریکا با ۷ ساعت معامله در روز؛ کالاهایی که از ۱
سپتامبر کندل ندارند) و ۹ از ۱۸ ترکیب مغز × منبع قیمت نماد دیگری انتخاب
می‌کردند. بازار بسته بود و هیچ تصمیمی روی آن گرفته نشد. `ad45374` نمادهای
کوتاه را بدون بازه دوباره می‌خواند؛ مقایسه با کد بدون بازه روی دادهٔ واقعی:
**صفر تفاوت** در همهٔ مغزها و هر دو منبع.

**علت اصلی باقی کندی پیدا و رفع شد:** پروفایل روی سرور نشان داد
`ingest_broker_bars` ۲۲۷ از ~۲۳۰ ثانیه را می‌گرفت — هر چرخه ~۳۸ هزار کندل
بروکر را که تقریباً همه تغییری نکرده بودند دوباره upsert می‌کرد. حالا فقط
کندل‌هایی نوشته می‌شوند که از «جدیدترین کندل ذخیره‌شده منهای ۵ کندل» تازه‌ترند
(`REWRITE_BARS`). پس از نصب کندل‌های H1/M15/M5 بروکر همچنان می‌رسند.
**بهای این انتخاب (تصمیم مالک):** اگر بروکر قیمت کندلی قدیمی‌تر از ۵ کندل
آخر را اصلاح کند، دیگر ثبت نمی‌شود.

همان روز ۲۱ فایل کندل هفت نمادی که پل دیگر منتشر نمی‌کند از پل mt5j به
`/root/bridge-j-stale-bars-20260913` روی سرور منتقل شدند (حذف نشدند)؛ در هر
چرخه خوانده می‌شدند.
| جهان معاملاتی | ۲۰ ابزار، رتبه‌بندی مالک |
| سرور | `178.33.150.51` |
| معاملهٔ واقعی | صفر |
| لبهٔ اثبات‌شده | هیچ |

## ناوگان (مغزها از گزارش سلامت ۱۳ سپتامبر؛ موجودی‌ها از ۷ سپتامبر)

| ترمینال | حساب | بروکر | موجودی | مغز |
|---|---|---|---|---|
| term-b | 67209473 | RoboForex-ECN | ۲۰۰٬۰۰۰ | trend-following |
| term-c | 67209482 | RoboForex-ECN | ۲۰۰٬۰۰۰ | short-horizon-reversal |
| term-d | 67209480 | RoboForex-ECN | ۲۰۰٬۰۰۰ | stochastic-reversion |
| term-e | 67209483 | RoboForex-ECN | ۲۰۰٬۰۰۰ | rsi-mean-reversion |
| term-f | 67209485 | RoboForex-ECN | ۲۰۰٬۰۰۰ | trend-following |
| term-g | 67209487 | RoboForex-ECN | ۲۰۰٬۰۰۰ | trend-following |
| **term-j** | **1514533027** | **FTMO-Demo** | ۲۰۰٬۰۰۰ | trend-following |

**تخصیص مغزها از ۷ سپتامبر عوض شده:** term-b از cross-sectional-stretch،
term-g از donchian-breakout و term-j از پیش‌فرض، هر سه به trend-following
رفته‌اند؛ حالا چهار حساب از هفت یک مغز دارند. این سند دلیل تغییر را ثبت نکرده
است. هندسه: استاپ ۷.۵×ATR، تارگت ۱.۵R، ریسک ۱٪ (تا ۱۳ سپتامبر ۰.۷۵٪).

همه دمو (`trade_mode: 0`)، لوریج ۱:۱۰۰، حالت هجینگ.

**چرا همه ۲۰۰ هزار:** حدها درصدی‌اند و طلا تعیین‌کننده است — با استاپ اندازه‌گیری‌شدهٔ
‎$۱۱۱.۸ روی XAUUSD، یک لات حداقلی ‎$۱۱۱.۸ ریسک دارد، که در ریسک ۰.۷۵٪ یعنی حداقل
‎$۱۴٬۹۰۰ موجودی فقط برای اینکه طلا قابل معامله باشد. و یکسان بودنشان شرط مقایسهٔ
مغزهاست: روی ناوگان قبلی موجودی‌ها از ۵۰ تا ۱۹۶٬۳۰۷ دلار فرق داشتند و دو حساب
حتی یک پوزیشن هم نمی‌توانستند بگیرند.

**دو مغز عمداً معامله نمی‌کنند** و فقط ثبت می‌کنند: `time-series-momentum`
(‎−۰.۰۳۰ R، t=−۲.۴۱ روی ۲۱ سال) و `carry-differential` (دههٔ اول مثبت، دورهٔ
داوری ‎−۰.۰۲۷ R با t=−۲.۱۸ — لبهٔ رژیم‌محور).

## چالش FTMO

`ftmo-challenge-2step-phase2` — هدف ۵٪ (‎$۱۰٬۰۰۰)، افت روزانه ۳٪ (‎$۶٬۰۰۰)،
افت کل ۱۰٪ (‎$۲۰٬۰۰۰) و تریل‌کننده، حداقل ۴ روز معامله. `currency_per_r` برابر
‎$۱٬۵۰۰ است — مشتق از ۰.۷۵٪ ریسکِ همین استقرار روی ۲۰۰ هزار، نه عددی که کسی
انتخاب کرده باشد.

**تأیید قوانین بر مبنای دمو بودن داده شد، نه بر مبنای خواندن قرارداد.** مالک
دنبال بند معاملهٔ خودکار گشت و پیدایش نکرد. پنج قانون دیگر (سقف لوریج، سقف
پوزیشن همزمان، معاملهٔ خبری، نگهداری آخر هفته، قانون consistency) وارد نشده‌اند و
به‌صورت «بررسی نشد» گزارش می‌شوند. **اگر این rulebook روزی به حساب فاندشده وصل
شد، آن حساب تأیید مستقل از قرارداد خودش لازم دارد.**
## وضعیت ایمنی (خوانده‌شده از کانتینر زنده، ۷ سپتامبر)

```
execution        : true      موتور روشن — تصمیم مالک
dry_run          : false     سفارش واقعاً به ترمینال می‌رود
real money       : false     MOLIDO_ALLOW_REAL_MONEY_ORDERS خاموش؛ دروازهٔ
                             حساب هر اکانتی را که ترمینال «واقعی» گزارش
                             کند رد می‌کند — سوییچ جدا، عمداً
kill switch      : disengaged   (فایل روی هاست؛ deploy پاکش نمی‌کند)
consensus        : 1         یک مغز برای اقدام کافی است
accounts         : شش RoboForex-ECN + یک FTMO-Demo، همه demo
broker adapter   : MetaTrader (پل فایل‌محور زیر Wine) + شبیه‌ساز paper
```

**دربارهٔ `consensus: 1`** — این سند تا امروز عدد ۲ را می‌نوشت و غلط بود. با
طراحی فعلی هر حساب **یک** مغز دارد، پس شرط توافق دو مغز یعنی هیچ حسابی هرگز
سفارش نمی‌فرستد. تضاد بین مغزها همچنان گرفته می‌شود، ولی در دروازهٔ سفارش و با
وزن آرا، نه با وتوی یک مغز: یک لانگ و یک شورت روی یک نماد در یک لحظه هر دو رد
می‌شوند، و سمتی که مغزهای بیشتری پشتش باشند برنده است.

هیچ اعتبارنامهٔ بروکری در هیچ‌جای این مخزن یا سرور نیست. `infra/.env.prod`
(رمز دیتابیس) فقط روی سرور است و در گیت نیست. رمز حساب‌های بروکر از فرم سایت
مستقیم به agent روی هاست می‌رود و در متاتریدر اعمال می‌شود؛ نه ذخیره می‌شود نه
لاگ.

## آنچه داده واقعی تا امروز گفته است

**هشدار: ژورنال در ۶ شهریور به درخواست مالک صفر شد.** ۹۴٬۳۴۲ ردیف حذف شدند
(پشتیبانِ بازیابی‌تست‌شده `molido-20260906T160546Z.dump` روی سرور). هر عددی که
قبلاً اینجا از ژورنال زنده نقل شده بود، دیگر پشتوانه‌ای در دیتابیس ندارد و
برداشته شد. آنچه می‌ماند از سنجش تاریخی ۲۱ ساله است، که دست‌نخورده باقی است.

### سه مغز روی همان داده، همان ترازو (سنجش تاریخی، دست‌نخورده)

| مغز | آموزش ۲۰۰۵–۲۰۱۷ | داوری ۲۰۱۷–۲۰۲۵ |
|---|---|---|
| cross-sectional-stretch | ‎+۰.۰۱۰ (t=۰.۸) | ‎**+۰.۰۷۰ (t=۴.۶۴)** |
| short-horizon-reversal | ‎−۰.۰۰۶ (t=−۰.۷) | ‎+۰.۰۱۹ (t=۱.۶۱) |
| time-series-momentum | ‎−۰.۰۰۷ (t=−۰.۷) | ‎**−۰.۰۳۰ (t=−۲.۴۱)** |
| carry-differential | ‎**+۰.۰۳۳ (t=+۲.۹۴)** | ‎**−۰.۰۲۷ (t=−۲.۱۸)** |

عدد بزرگ قاعدهٔ مقطعی با احتیاط خوانده شود: دورهٔ آموزشش هیچ بود و دورهٔ مثبتش
با دورهٔ طراحی خودِ قاعده هم‌پوشان است.

### هزینهٔ معامله، اندازه‌گیری‌شده روی H1 (۷ روز، پیش از صفر شدن)

| نماد | فاصلهٔ استاپ | ریسک با حداقل حجم |
|---|---|---|
| AUDUSD | ۴۹ پیپ | ‎$۴.۹ |
| EURUSD | ۵۴ پیپ | ‎$۵.۴ |
| USDJPY | ۲۵۸ پیپ | ‎$۱۶.۵ |
| GBPJPY | ۳۲۲ پیپ | ‎$۲۰.۶ |
| XAUUSD | ‎$۱۱۱.۸ | ‎$۱۱۱.۸ |

این جدول است که اندازهٔ ۲۰۰ هزار دلاری حساب‌ها را تعیین کرد.

### ژورنال زنده

۱۸٬۴۷۲ ردیف ژورنال از ۶ شهریور (۱۳ سپتامبر)؛ تعداد حل‌شده‌ها شمرده نشد.
**هیچ حکمی از این عدد گرفته نشود.**
آستانه همان است که بود: تا ~۵۰ معاملهٔ حل‌شده به ازای هر مغز، هیچ نتیجه‌ای.
## معماری در یک نگاه

```
bars → features → regime → council → adversary → levels(ATR)
     → calibrated probability → EV → portfolio → risk → stress
     → challenge → OrderIntent → [execution gate: بسته]
```

- هر خواندن تاریخی از `point_in_time.get_bars` می‌گذرد؛ replay اثبات‌شده
  deterministic است (تستی که بازبینیِ دیرآمده را تزریق می‌کند و برابری
  می‌خواهد).
- عدم‌قطعیت فقط ریسک را **کم** می‌کند؛ هیچ ورودی‌ای هیچ ماژولی را
  سهل‌گیرتر نمی‌کند.
- «نامعلوم» از «صفرِ اندازه‌گیری‌شده» در تمام لایه‌ها تفکیک‌پذیر است.
- تلگرام/n8n فقط می‌توانند بپرسند؛ مسیری از پیام ورودی به موتور اجرا
  وجود ندارد (با parse کردن import ها تست شده).

## محدودیت‌های دانسته

1. **آداپتور بروکر واقعی وجود ندارد** — عمداً. پروتکل و شبیه‌ساز کامل‌اند؛
   اتصال واقعی نیازمند تصمیم صریح مالک، کلید روی سرور، و `require_auth=true`.
2. **rulebook هیچ پراپ‌فرمی کامل وارد نشده** — ۱۱ rulebook رونویسی شده،
   و شش قانون در همه‌شان `None` است، یعنی «کسی وارد نکرده».

   | قانون | وضعیت روی FTMO فاز ۲ |
   |---|---|
   | `max_leverage` | وارد نشده — گزارش می‌شود، نمی‌بندد |
   | `max_concurrent_positions` | وارد نشده — گزارش می‌شود، نمی‌بندد |
   | `weekend_holding_allowed` | وارد نشده — گزارش می‌شود، نمی‌بندد |
   | `news_trading_allowed` | وارد نشده — گزارش می‌شود، نمی‌بندد |
   | consistency | وارد نشده — گزارش می‌شود، نمی‌بندد |
   | `automated_trading_allowed` | وارد نشده — **می‌بندد**، مگر با تأیید مالک |

   **تصحیح (۷ سپتامبر):** این سند می‌گفت «هر کدام به‌تنهایی دروازه را
   می‌بندد». روی حساب زنده آزموده شد و درست نبود: پنج قانون اول فقط گزارش
   می‌شوند («وارد نشده، پس بررسی نشد»). آنچه واقعاً می‌بست `currency_per_r`
   بود — بدون دانستن اینکه یک R چند دلار است، ریسک جدید در برابر حدهای افت
   قابل سنجش نیست.

   `automated_trading_allowed` همچنان تنها قانونی است که نامعلوم بودنش
   **حجم را کم نمی‌کند، متوقف می‌کند** — چون پرسش این است که آیا نرم‌افزار
   حق انتخاب معامله را دارد. تأیید مالک آن را باز می‌کند، و آن تأیید ادعایی
   دربارهٔ قرارداد اوست نه چیزی که از صفحهٔ عمومی قابل استنتاج باشد.
3. ~~در `ChallengeRules` برای قوانین عددی `None` هم «تحمیل نشده» و هم
   «نامشخص» است~~ — **این دیگر درست نیست** (اصلاح ۹ شهریور ۱۴۰۵). سنتینل
   `NOT_IMPOSED` از ۲۲ مرداد برای هر سه نوع قانون کار می‌کند
   (`Rule = float | NotImposed | None`)، نه فقط بولی‌ها؛ هیچ تغییر schema
   لازم نبود. این بند از زمانی که حل شده بود در سند مانده بود.
4. ~~**تصمیم روی مقطعی که بازار از آن گذشته — نگهبان ندارد.**~~ — **رفع شد**
   (۷ سپتامبر، همان روز). هر نمادی که کندلی بعد از مقطع رتبه‌بندی داشته باشد
   از رتبه‌بندی کنار می‌رود و در گزارش چرخه با نام شمرده می‌شود
   (`already_answered`). per-instrument است نه per-cycle: کهنه بودن کل کتاب
   با هم یعنی بازار بسته و همچنان رتبه‌بندی می‌شود.

   دو شکل غلط پیش از این امتحان و رد شد: رد کردن بر مبنای ساعت دیوار، و رد
   کردن هر وقت کندل جدیدتری وجود دارد. هر دو رتبه‌بندی آخر هفته را می‌شکستند،
   که عمدی است و تست دارد.
4. **ارزیابی lot-size و margin واقعی** پیاده نشده — تا بروکر واقعی نباشد،
   قابل اندازه‌گیری نیست و شبیه‌سازی‌اش دروغ می‌شد.
5. دیسک سرور ۹۷GB است و ۱۳GB آزاد دارد (۸۷٪ پر — هر prefix متاتریدر ~۴.۵GB و هفت‌تا فعال است)؛ کش build داکر پس از هر deploy سقف ۲GB می‌خورَد و
   جدول‌های عملیاتی retention دارند — با این دو، مصرف پایدار است (~۱GB در سال).

## قدم بعدی (به ترتیب ارزش)

1. **ژورنال زنده را بگذارید پر شود.** هفت حساب، هرکدام یک مغز، از صفرِ
   ۶ شهریور. کارنامهٔ هفتگی (`python -m app.learning.weekly`، یکشنبه‌ها ۰۹:۳۰
   UTC) و checkpoint هر شش ساعت روی هاست گزارش می‌دهند. تا ~۵۰ معاملهٔ
   حل‌شده به ازای هر مغز، هیچ حکمی نگیرید.
2. **نخستین حل‌شدن‌ها را تأیید کنید.** حل‌کننده در ۳ سپتامبر مرده بود و
   ۶ سپتامبر رفع شد؛ ژورنال از آن زمان صفر است، پس `resolutions` در گزارش
   سلامت تا اولین معاملهٔ بسته‌شده قرمز می‌ماند. اگر پس از یک روزِ بازارِ باز
   هنوز صفر بود، نقص است نه انتظار.
3. **نقشهٔ ساعت طلایی**: لبهٔ هر مغز به تفکیک جلسهٔ معاملاتی روی H1. اگر
   جلسه‌ای معنادار شد، فیلتر ساعتی به دروازه‌ها اضافه می‌شود — حذف مستند،
   نه حدس.
4. **~~ورود پنج قانون واردنشدهٔ rulebook توسط مالک~~ — چهارتا وارد شد،
   ۸ سپتامبر ۲۰۲۶.** هیچ‌کدام منتشرنشده نبود؛ خوانده‌نشده بود. روی FTMO:
   `automated_trading_allowed=True`، `max_concurrent_positions=200`، و
   خبر و نگهداری آخر هفته **به تفکیک فاز** — در کل دورهٔ ارزیابی آزاد، روی
   حساب فاندشده محدود. `max_single_day_profit_share=0.50` فقط روی 1-Step.

   دو قانون آخری دلیل این بود که جست‌وجوی قبلی چیزی پیدا نکرد: خاصیت *فاز*
   بودند نه خاصیت شرکت، و یک پرچم واحد به‌ازای هر ارائه‌دهنده نمی‌توانست
   نگهشان دارد.

   **`max_leverage` هنوز وارد نشده و عمداً.** FTMO آن را در هیچ صفحهٔ
   عمومی منتشر نمی‌کند؛ هنگام خرید به‌ازای نوع حساب تعیین می‌شود. عددی که
   همه‌جا تکرار می‌شود ۱:۱۰۰ است و نوشتنش با آن اعتبار، دقیقاً همان
   ساختنی است که این فایل برای ردش وجود دارد.
5. **سقف پوزیشن در برابر جهان ۲۰ ابزاره** را زیر نظر بگیرید: روی ناوگان قبلی
   دو حساب سر سقف ۱۰ پوزیشن ایستاده بودند و ۹ جای اشغال‌شده مال ابزارهایی بود
   که دیگر اجازهٔ معامله ندارند. با ناوگان تازه هنوز پیش نیامده.
6. یک کلید منتظر مالک: `MOLIDO_ANTHROPIC_API_KEY` (تحلیلگر — لایهٔ تفسیری
   Claude). تلگرام دیگر منتظر نیست: از فرم سایت پیکربندی شده، توکن معتبر است و
   دایجست روزانه تحویل داده می‌شود.
## اصل حاکم

> عددی که ساخته شود، از عددی که وجود ندارد خطرناک‌تر است. هر لایه اجازه
> دارد بگوید «نمی‌دانم» و موظف است دلیلش را بگوید؛ هیچ لایه‌ای اجازه ندارد
> حدس بزند.
