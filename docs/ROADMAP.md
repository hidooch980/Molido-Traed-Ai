# نقشهٔ راه — امنیت، مغز دوم، رابط کاربری و استقرار

*نوشته‌شده: ۳ شهریور ۱۴۰۵ (25 Aug 2026)*

این سند هفت فاز است که به ترتیب اجرا می‌شوند. هر فاز وقتی «تمام» است که
ruff و mypy پاک باشند و تست‌هایش سبز — نه وقتی کدش نوشته شده باشد.

## آنچه از قبل هست (و دوباره ساخته نمی‌شود)

بخش بزرگی از زیرساخت امنیتی وجود دارد و فازهای زیر روی آن سوار می‌شوند،
نه به‌جای آن:

| هست | کجا |
|---|---|
| هش رمز و کلید API، مقایسهٔ زمان‌ثابت | [`core/security.py`](../backend/app/core/security.py) |
| ورود با نشست و کوکی، انقضا | [`services/sessions_auth.py`](../backend/app/services/sessions_auth.py) |
| پنج نقش، سه مجوز، `Principal` واحد | [`api/deps.py`](../backend/app/api/deps.py) |
| دروازه‌ای که اپ را بالا نمی‌آورد اگر مسیر تغییردهندهٔ بی‌مجوز باشد | [`api/guard.py`](../backend/app/api/guard.py) |
| ثبت رویداد و ژورنال | [`services/audit.py`](../backend/app/services/audit.py) |
| ساخت اولین مالک، ثبت‌نام، تغییر رمز | [`api/v1/users.py`](../backend/app/api/v1/users.py) |
| PWA نصب‌شدنی (manifest و آیکون) | [`frontend/src/app/manifest.ts`](../frontend/src/app/manifest.ts) |

## آنچه نیست

محدودیت نرخ ورود، قفل پس از تلاش ناموفق، «ربات نیستم»، ورود دومرحله‌ای،
مجوز ریزدانه، دفاع لبه، service worker، مغز دوم.

---

## فاز A — مجوزهای ریزدانه و یوزر مالک ✅ انجام شد

**مسئله.** امروز `OWNER`، `ADMIN` و `TRADER` **دقیقاً یک مجموعه مجوز
دارند**: هر سه `READ | SIMULATE | EXECUTE`. یعنی نقش‌ها در پایگاه‌داده از هم
جدا هستند و در عمل هیچ فرقی نمی‌کنند — کسی که برای «تحلیل» اضافه شده،
می‌تواند سفارش بفرستد.

**کار.** مجوزها ریزدانه می‌شوند (مدیریت کاربر، مدیریت کلید، اتصال بروکر،
کلید توقف، نوشتن تنظیمات، نوشتن rulebook، خواندن گزارش حسابرسی، اجرا،
شبیه‌سازی، خواندن). سه تای قدیمی باقی می‌مانند تا مسیرهای موجود نشکنند و
`guard` همچنان کار کند.

**نتیجه برای مالک.** یک نقش `OWNER` که همه‌چیز را دارد، و بقیهٔ نقش‌ها که
هرکدام دقیقاً چیزی را دارند که لازم دارند.

## فاز B — سخت‌سازی ورود ✅ انجام شد (جز ورود دومرحله‌ای)

- محدودیت نرخ روی `sign-in` و `claim` و `register`، بر اساس IP و بر اساس
  نام کاربری — هر دو، چون یکی‌شان تنها با یک بات‌نت یا یک لیست کاربر دور
  زده می‌شود.
- قفل تصاعدی پس از تلاش ناموفق، با پیام یکسان برای «کاربر نیست» و «رمز
  غلط» تا شمارش کاربر ممکن نباشد.
- «ربات نیستم» روی مسیرهای عمومی.
- ورود دومرحله‌ای (TOTP) برای نقش‌هایی که مجوز اجرا دارند. — **باقی مانده**

سقف قفلِ حساب عمداً کوتاه است (۱۰ دقیقه). کلید توقف از داشبوردِ واردشده
در دسترس است، پس یک ساعت قفل یعنی یک ساعت ناتوانی از متوقف‌کردن معاملات
خودتان — و مهاجمی که رمز را نمی‌داند می‌تواند همان را با شکست عمدی
بسازد. قفل طولانی روی **آدرس تماس‌گیرنده** است، جایی که اشتباه‌بودنش
فقط یعنی تغییر شبکه.

## فاز C — لاگ امنیتی

هر رویداد امنیتی با هویت، IP، عامل کاربر و نتیجه: ورود موفق، ورود ناموفق،
قفل، رد مجوز، ساخت و حذف کلید، تغییر نقش، درگیر و آزادشدن کلید توقف.
قابل خواندن از داشبورد، با مجوز `AUDIT_READ` — نه برای همه.

## فاز D — مغز دوم، بخش زبانی

یک تحلیلگر مبتنی بر Claude API که `trace` تصمیم را می‌خواند و به فارسی
می‌گوید **چرا** معامله نشد، و به تصمیم اعتراض می‌کند. سه قاعده:

1. **معامله نمی‌کند.** هیچ مسیری از خروجی این ماژول به موتور اجرا نیست.
2. **عدد نمی‌سازد.** اگر داده‌ای نبود، می‌گوید نبود.
3. **خروجی‌اش ثبت می‌شود** تا بعداً بتوان سنجید حرف‌هایش درست بود یا نه.

## فاز E — مغز دوم، بخش کمّی

خانوادهٔ استراتژی دوم با منطق مستقل، و یک **دروازهٔ توافق**: معامله فقط
وقتی مجاز است که هر دو مغز موافق باشند. این تنها فازی است که می‌تواند
لبهٔ اثبات‌نشدهٔ فعلی (z = ۱.۱۰ در برابر ۱.۹۶ لازم) را جابه‌جا کند.

## فاز F — رابط کاربری و PWA

بازطراحی کامل ۳۴ صفحه و المان‌ها، ساده‌کردن دسترسی، و PWA کامل:
service worker، کار آفلاین برای صفحات خواندنی، نوتیفیکیشن.

## فاز G — استقرار روی VPS جدید

نصب از صفر، دامنه و TLS، فایروال، fail2ban، دفاع لبه در برابر DDoS،
بکاپ تأییدشده، و به‌روزرسانی با یک دستور.

**یافتهٔ مهم دربارهٔ `trade.molido.shop`.** دامنه به دو آدرس
`185.143.233.238` و `185.143.234.238` اشاره می‌کند و هدر پاسخ
`Server: ArvanCloud` است — یعنی دامنه پشت CDN آروان است، نه مستقیم روی
سرور. سه پیامد که پیکربندی را عوض می‌کند:

1. **TLS را آروان تمام می‌کند.** چالش HTTP-01 لِتس‌انکریپت روی Caddy از
   پشت CDN جواب نمی‌دهد مگر عبور داده شود. یا گواهی را آروان بدهد و
   Caddy روی HTTP داخلی بماند، یا رکورد به‌صورت DNS-only تنظیم شود.
2. **`MOLIDO_TRUSTED_PROXY_HOPS` باید ۲ باشد، نه ۱** — آروان و بعد Caddy.
   با ۱، آدرسی که شمرده می‌شود آدرسِ آروان است و همهٔ دنیا در یک سطل
   می‌افتند؛ با ۰، همان و بدتر.
3. **دفاع DDoS لبه از قبل هست.** این دقیقاً همان لایه‌ای است که در
   اپلیکیشن ساختنی نبود.

همچنین همین حالا چیزی روی آن دامنه پاسخ ۲۰۰ می‌دهد (۵۵ کیلوبایت HTML،
آخرین تغییر ۶ اوت). پیش از استقرار باید معلوم شود آن چیست و جایگزین
می‌شود یا نه.

---

## یک چیز که وعده داده نمی‌شود

«نفوذناپذیر» وجود ندارد. آنچه ساخته می‌شود لایه‌هایی است که نفوذ را گران و
پرسروصدا می‌کند. همچنین DDoS واقعی در لایهٔ اپلیکیشن متوقف نمی‌شود؛ باید در
لبهٔ شبکه (مثلاً Cloudflare جلوی سرور) بایستد. هر ادعای دیگری در این سند
دروغ می‌بود.
